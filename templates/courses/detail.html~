<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>PythonBursa</title>
    <!-- Bootstrap -->
    {% load staticfiles %}
    <link href="{% static 'css/bootstrap.css' %}" rel="stylesheet">
+   <link href="{% static 'css/style.css' %}" rel="stylesheet">
    <script src="{% static 'js/bootstrap.js' %}"></script>
    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
  <body>
    <hr class="topping_index"></hr>
    <nav class="navbar navbar-inverse navbar-fixed-top">
      <div class="container-fluid">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="{% url 'index' %}">Главная</a>
        <a class="navbar-brand" href="{% url 'contact' %}">Контакты</a>
        <a class="navbar-brand" href="{% url 'students:list_view' %}?course_id={{courses.id}}">Студенты</a>
      </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>
    <div class="jumbotron">
      <div class="container">
	<div class="bursa">
          <h1 id="pb" class="bursa">{{ courses.name }}</h1>
          <h2 class="des">Описание курса</h2>
          <p class="descr">{{ courses.description }}
          </p>
        </div>
      </div>
    </div>
    <div class="course_id" >
    <div class="panel panel-default">
  <!-- Default panel contents -->
      <div class="panel-heading"><h2 class="py_program"></h2></div>

            
             
  <!-- Table -->
        <table class="table-responsive">
                   
          <tr class="headtable"><td><h3>#</h3></td><td><h3>Тема недели</h3></td><td><h3>Описание</h3></td><tr>
              {% for lesson in lessons %}
  <tr class="tablelist"><td><h3>{{ lesson.order }}</h3></td><td><div class="them"><h3>{{ lesson.subject }}</h3></div></td><td><h3>{{ lesson.description }}</h3></td></tr>
            {% endfor %}
         </table>
       </div>
</div>
</div>
    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
  </body>
</html>
