{% extends "base.html" %}


{% load staticfiles %}


{% block title %}Coaches information{% endblock title %}


{% block content %}
		<div class="container">
			<div class="page-header">
				<h2>{{ teacher.name }} {{ teacher.surname }}</h2>
				<h4>Информация:</h4>
			</div>
			<div class="row">
				<div class="col-md-7">
					<table class="table table-striped">
						<thead>
							<tr>
				      <th>дата рождения</th>
				      <td>{{ teacher.date_of_birth|date:'F j, Y' }}</td>
						</tr>
						<tr>
							<th>адрес</th>
							<td>{{ teacher.address }}</td>
						</tr>
				    <tr>
							<th>почта</th>
							<td>{{ teacher.email }}</td>
						</tr>
						<tr>
							<th>логин skype</th>
							<td>{{ teacher.skype }}</td>
						</tr>
						<tr>
							<th>телефон</th>
							<td>{{ teacher.phone }}</td>
						</tr>
						{% if teacher.coach_courses.all %}
						<tr>
							<th>курсы учитель</th>
							<td>
								{% for course in teacher.coach_courses.all %}								
								<p><a href="{% url 'courses:detail' course.id %}">{{ course.name }}</a> студенты курса <a href="{% url 'students:list_view' %}?course_id={{course.id}}"><span class="glyphicon glyphicon-share" aria-hidden="true"></span></a></p>
								{% endfor %}
							</td>
						</tr>
						{% endif %}
						{% if teacher.assistant_courses.all %}
						<tr>
							<th>курсы ассистент</th>
							<td>
								{% for course in teacher.assistant_courses.all %}
								<p><a href="{% url 'courses:detail' course.id %}">{{ course.name }}</a> студенты курса <a href="{% url 'students:list_view' %}?course_id={{course.id}}"><span class="glyphicon glyphicon-share" aria-hidden="true"></span></a></p>
								{% endfor %}
							</td>
						</tr>
						{% endif %}
					</table>
				</div>
			</div>
		</div>
{% endblock content %}
		
