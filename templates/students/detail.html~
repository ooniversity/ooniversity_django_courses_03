{% extends "base.html" %}

{% load staticfiles %}


{% block title %}<title>ooniversity-students_details</title>{% endblock %}


{% block nav_brand_students %}active{% endblock %}


{% block content %}
  <div class="page-header">
                             {% for student in students_on_course %}
    <h1 class="student-head">{{ student.name}} {{student.surname }}</h1>
      <p class="below-header">Выпуск 2015</p>                         
  </div>
	<div class="study_table2">
		<div class="panel panel-default">
  		<div class="panel-heading"><h2 class="course_program" >информация о студенте</h2></div>
      	<table class="table table-hover">
        	<tr><td><h3>
                      дата рождения
            </h3></td>
            <td><h3>
                      {{ student.date_of_birth }}
            </h3></td></tr>
          <tr><td><h3>
                      адрес
            </h3></td>
            <td><h3>
                      {{ student.address }}
            </h3></td></tr>
          <tr><td><h3>
                      email
            </h3></td>
            <td><h3>
                      {{ student.email }}
            </h3></td></tr>
          <tr><td><h3>
                      skype
            </h3></td>
            <td><h3>
                      {{ student.skype }}
            </h3></td></tr>
          <tr><td><h3>
                      телефон
            </h3></td>
            <td><h3>
                      {{ student.phone }}
            </h3></td></tr>
          <tr><td><h3>
                      курсы
            </h3></td>
            <td><h3>
                      {% for course in student.courses.all %}
            <a href="{% url 'courses:detail' course.id %}">{{ course.name }}</a></br>
					            {% endfor %}
            </h3></td></tr>
          </table>
        </div>
      </div>
                      {% endfor %}
{% endblock content %}    
