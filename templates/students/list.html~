{% extends "base.html" %}


{% load staticfiles %}


{% block title %}ooniversity-students_list{% endblock ttle %}


{% block nav_brand_students %}active{% endblock %}


{% block content %}
  <div class="page-header">
    <h1 class="student-head">Студенты ITBursa</h1>
      <p class="below-header">Список студентов учащихся в ooniversity</p>
  </div>
  <div class="study_table2">
	  <div class="panel panel-default">
  	  <div class="panel-heading"><h2 class="course_program" >курс 2015</h2></div>
		    <table class="table table-hover">
  		    <tr class="headtable_student"><td><h3>No</h3></td><td><h3>фамилия Имя</h3></td><td><h3>адрес</h3></td><td><h3>skype</h3></   td><td><h3>курсы</h3></td><tr>
            {% for student in students_on_course %}            
          <tr ><td><h3>{{student.id}}</h3></td><td><h3><a href="{% url 'students:detail' student.id %}">{{ student.surname }} {{ student.name }}</a></h3></td><td><h3>{{student.address}}</h3></td><td><h3>{{ student.skype }}</h3></td><td><h3>{% for course in student.courses.all %}<a href="{% url 'courses:detail' course.id %}">{{ course.name }}{% endfor %}</a></h3></td></tr>
            {% endfor %}   
			  </table>
      </div>
    </div>
{% endblock content %}
