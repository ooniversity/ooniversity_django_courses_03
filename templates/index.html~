{% extends "base.html" %}
{% load staticfiles %}

{% block title %}PyBursa Main page{% endblock title %}


{% block nav_class_index %}active{% endblock %}


{% block url_student %}{% url 'students:list_view' %}{% endblock url_student %}


{% block content %}
<div class="jumbotron">
  <h1>HogwBursa</h1>        
  <h2>Description</h2>
  <p class="lead">Язык программирования Python (волш. Василиск) — это мощный инструмент для создания программ самого разнообразного назначения, доступный даже для маглов.  </p>
</div>
      
 <div class="jumbotron">
{% if messages %}

            <ul class="messages">

                {% for message in messages %}

                <li{% if message.tags %} class="{{ message.tags }}"{% endif %}>

                    {% if message.level == DEFAULT_MESSAGE_LEVELS.ERROR %}Important: {% endif %}

                    {{ message }}

                </li>

                {% endfor %}

            </ul>

          {% endif %}

   <p>      

   <a href="{% url 'courses:add' %}">Добавить новый курс</a>
   </p>
  <h3>Kurs</h3>
  <table class="table">
  <tr><th>Курс </th> <th>Описание</th> </tr>
  {% for course in courses %} 
  <tr> <td><a href="{% url 'courses:detail' course.id %}">{{ course|upper }} </a> </td> <br> <td>{{ course.short_description|title }} </td>
<td><a href="{% url 'courses:edit' course.id %}">Изменить</a></td></tr>
  {% endfor %}
  </table>
</div>
{% endblock content %}
