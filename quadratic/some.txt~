form = QForm(request.GET)
def vdfv(request):
	if form.is_valid():
		что-то делаем
	else:
		форма с ошибками


GET Запрос!!!
старая валидация должна уйти!
но проверка например на буквы должна быть/
атрибут error
